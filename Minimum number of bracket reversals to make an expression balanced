class Solution {
public:
      int countRev (string s) {
  if(s.size()&1)return -1;

  int o=0, cl=0;
  int i; int n= s.size();

  for(i=0; i<n; i++){
    if(s[i]=='(')o++;
    else{
        if(o>0)o--;
        else cl++;
    }
  }

  return (o/2)+(o%2) + (cl/2) + (cl%2);
    }
};
